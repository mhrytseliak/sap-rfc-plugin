# sap-disconnect

Disconnect from SAP and remove all stored credentials.

## Auto-trigger keywords
disconnect SAP, remove SAP connection, SAP logout, clear SAP credentials

## What it does
1. Removes MCP server registration
2. Deletes all credentials from OS keyring
